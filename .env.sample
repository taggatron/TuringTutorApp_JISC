# Copy this file to .env (recommended) or APIkey.env and fill values.
# The app currently reads from APIkey.env for backward compatibility.
OPENAI_API_KEY=your_openai_api_key_here
SESSION_SECRET=replace_me_with_strong_random
NODE_ENV=development

# Server
PORT=3000
# Enable HTTPS (true/false). When true, also set SSL key/cert paths below.
HTTPS_ENABLED=false
SSL_KEY_PATH=/absolute/path/to/localhost-key.pem
SSL_CERT_PATH=/absolute/path/to/localhost-cert.pem

# Database (Postgres - planned migration)
PGHOST=localhost
PGPORT=5432
PGDATABASE=turing_tutor
PGUSER=app_user
PGPASSWORD=your_password_here
# Optional: unified connection string
# DATABASE_URL=postgres://app_user:your_password_here@localhost:5432/turing_tutor
