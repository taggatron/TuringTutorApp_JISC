# Copy this file to APIkey.env or .env and fill in the values
# The app currently reads from APIkey.env for backward compatibility.
# You can switch to .env by updating dotenv.config({ path: './.env' }) in streaming.js

OPENAI_API_KEY=
SESSION_SECRET=please-change-this
NODE_ENV=development
